# Changelog

All notable changes to this project will be documented in this file.

---

## [0.1.0] - 2026-03-21
### Added
- Initial project structure using modern `src/` layout
- Core detection engine (`PromptInjectionDetector`)
- Pattern-based detector module
- Heuristic-based detector module
- Configuration system (`DetectorConfig`)
- Data models (`DetectionResult`, `Severity`)
- Command-line interface (`pid.cli`)
- Top-level project files (`README`, `LICENSE`, `pyproject.toml`, etc.)
- Empty `tests/` and `docs/` directories for future expansion